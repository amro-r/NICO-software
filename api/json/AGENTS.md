# Agents Overview – `json/`
- Motor and controller configuration for the NICO upper body (`nico_humanoid_upper.json`).
- Defines controller groups, motor IDs, limits, and orientation metadata used by higher-level motion stacks.

