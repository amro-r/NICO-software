# Agents Overview – `msg/`
- Custom ROS messages exported by the ELMiRA package.
- `DetectedObject.msg` describes OWLv2 detector outputs (label, score, bbox).
- `JointPosition.msg` wraps joint name/position arrays for IK results and motion execution.

