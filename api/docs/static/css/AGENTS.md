# Agents Overview – `docs/static/css/`
- CSS bundle for the documentation site.
- Contains vendor styles (Bulma, Bulma carousel/slider, Font Awesome) and project-specific rules in `index.css`.
- `bulma.css.map.txt` is included verbatim from the upstream theme for debugging.

