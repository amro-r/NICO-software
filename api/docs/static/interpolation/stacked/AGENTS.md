# Agents Overview – `docs/static/interpolation/stacked/`
- Empty directory reserved for generated interpolation frames that the website slider can display.
- Keeps structure compatible with the Nerfies-inspired template even when no frames are present.

