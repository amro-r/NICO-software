# Agents Overview – `docs/static/`
- Common asset bucket shared by the documentation site.
- Subdirectories separate styling (`css/`), scripts (`js/`), media (`images/`, `videos/`), and interpolation frame storage (`interpolation/`).
- Assets are referenced by `docs/index.html` and follow the Creative Commons license declared at the top level.

