# Agents Overview – `urdf/`
- URDF models for NICO left/right arms, including mesh references, inertial properties, and joint limits.
- Loaded by the EvoIK solver (`scripts/ik_solver.py`) to resolve arm trajectories for manipulation tasks.

